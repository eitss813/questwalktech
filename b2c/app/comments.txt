CREATE TABLE game_users( user_Id INT AUTO_INCREMENT, user_Fname VARCHAR(60), user_Lname VARCHAR(60), user_Email VARCHAR(120), user_UserName VARCHAR(60), user_Phone VARCHAR(60), user_Pic VARCHAR(120), user_Password VARCHAR(120), user_PasswordEncrypt VARCHAR(255), user_AccountVerification TINYINT DEFAULT 0 COMMENT "0-Pending for email verification, 1-email verified", user_CreatedBy INT COMMENT "creator id", user_CreatedMedium VARCHAR(120) COMMENT "user created by which medium like web(timestamp), csv(timestamp), api(timestamp) etc", user_AccountDurationStart DATETIME DEFAULT NULL COMMENT "User account duration start date", user_AccountDurationEnd DATETIME DEFAULT NULL COMMENT "User account duration end date", user_CreatedOn DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT "user account is created on this date", user_UpdatedOn DATETIME DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT "this column will be update auto[...]
ALTER TABLE `game_users` ADD `User_status` TINYINT NOT NULL DEFAULT '0' COMMENT '0-Active, 1-DeActive' AFTER `user_UpdatedOn`;
ALTER TABLE `game_users` CHANGE `user_CreatedBy` `user_CreatedBy` INT NULL DEFAULT NULL COMMENT 'creator id or -21 for self creation';
